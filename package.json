{
  "name": "cliks-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Cliks ambient coworking prototype: Vercel site, Render backend, and Go CLI.",
  "license": "MIT",
  "workspaces": [
    "server",
    "site",
    "cli"
  ],
  "scripts": {
    "dev:server": "npm --workspace @cliks/server run dev",
    "dev:site": "npm --workspace @cliks/site run dev",
    "dev:cli": "npm --workspace @cliks/cli run dev",
    "build": "npm --workspace @cliks/server run build && npm --workspace @cliks/site run build && npm --workspace @cliks/cli run build",
    "check": "npm --workspace @cliks/server run check && npm --workspace @cliks/site run check && npm --workspace @cliks/cli run check",
    "smoke:server": "node scripts/smoke-server.mjs",
    "load:server": "node scripts/load-test.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "ws": "^8.21.0"
  }
}
