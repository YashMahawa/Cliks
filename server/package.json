{
  "name": "@cliks/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run .",
    "build": "go build -trimpath -ldflags=\"-s -w\" -o dist/cliks-server .",
    "start": "./dist/cliks-server",
    "check": "go test ./..."
  }
}
